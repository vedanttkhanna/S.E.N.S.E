package com.aegisedge.os.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary (Google Teal) ──────────────────────────────────────
val SensePrimary = Color(0xFF00897B)          // Teal 600
val SensePrimaryLight = Color(0xFF4DB6AC)     // Teal 300
val SensePrimaryDark = Color(0xFF00695C)      // Teal 800
val SenseOnPrimary = Color(0xFFFFFFFF)

// ── Secondary (Google Blue) ────────────────────────────────────
val SenseSecondary = Color(0xFF1E88E5)        // Blue 600
val SenseSecondaryLight = Color(0xFF64B5F6)   // Blue 300
val SenseOnSecondary = Color(0xFFFFFFFF)

// ── Tertiary (Amber accent) ────────────────────────────────────
val SenseTertiary = Color(0xFFFF8F00)         // Amber 800
val SenseOnTertiary = Color(0xFFFFFFFF)

// ── Light Surfaces ─────────────────────────────────────────────
val LightBackground = Color(0xFFF8FAFB)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFF0F4F5)
val LightSurfaceContainer = Color(0xFFE8EDEF)
val LightOnBackground = Color(0xFF1A1C1E)
val LightOnSurface = Color(0xFF1A1C1E)
val LightOnSurfaceVariant = Color(0xFF44474E)
val LightOutline = Color(0xFF74777F)
val LightOutlineVariant = Color(0xFFC4C6D0)

// ── Dark Surfaces ──────────────────────────────────────────────
val DarkBackground = Color(0xFF0F1318)
val DarkSurface = Color(0xFF161B22)
val DarkSurfaceVariant = Color(0xFF1E252E)
val DarkSurfaceContainer = Color(0xFF252D38)
val DarkOnBackground = Color(0xFFE2E6EA)
val DarkOnSurface = Color(0xFFE2E6EA)
val DarkOnSurfaceVariant = Color(0xFFC1C6CE)
val DarkOutline = Color(0xFF8B9198)

// ── Semantic: Threat Levels ────────────────────────────────────
val ThreatBenign = Color(0xFF2E7D32)          // Green 800
val ThreatBenignBg = Color(0xFFE8F5E9)        // Green 50
val ThreatAmbiguous = Color(0xFF9E9E9E)       // Grey 500
val ThreatAmbiguousBg = Color(0xFFF5F5F5)     // Grey 50
val ThreatElevated = Color(0xFFEF6C00)        // Orange 800
val ThreatElevatedBg = Color(0xFFFFF3E0)      // Orange 50
val ThreatVerified = Color(0xFFC62828)         // Red 800
val ThreatVerifiedBg = Color(0xFFFFEBEE)       // Red 50

// ── Semantic: Dark threat levels ───────────────────────────────
val ThreatBenignDark = Color(0xFF66BB6A)
val ThreatElevatedDark = Color(0xFFFFB74D)
val ThreatVerifiedDark = Color(0xFFEF5350)

// ── Error ──────────────────────────────────────────────────────
val SenseError = Color(0xFFBA1A1A)
val SenseOnError = Color(0xFFFFFFFF)
val SenseErrorContainer = Color(0xFFFFDAD6)
val SenseOnErrorContainer = Color(0xFF410002)
